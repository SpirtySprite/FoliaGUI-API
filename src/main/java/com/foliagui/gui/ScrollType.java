package com.foliagui.gui;

/**
 * The direction a {@link ScrollingGui} scrolls its content.
 */
public enum ScrollType {

    /** Content advances one row at a time (top-to-bottom). */
    VERTICAL,

    /** Content advances one column at a time (left-to-right). */
    HORIZONTAL
}
